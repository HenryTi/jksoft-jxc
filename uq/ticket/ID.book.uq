
IDX Balance (
    id,                     -- biz subject
    init VALUE DEFAULT 0,
    value Value,
);

ID History (
    id MINUTE,
    subject ID,         -- 科目
    value Value,
    ref ID,             -- 相关凭证
    INDEX subject_id(subject, id) UNIQUE,
);
